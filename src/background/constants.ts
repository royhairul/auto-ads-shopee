// ==========================================================
// 🔹 Global Configurable Constants
// ==========================================================

// Interval waktu anti double update (dalam menit)
export const ANTI_DOUBLE_UPDATE_MINUTES: number = 3

// Default interval pengecekan campaign untuk createAlarm (menit)
export const DEFAULT_ALARM_INTERVAL_MINUTES: number = 1

// Default mode trigger
export const DEFAULT_MODE: string = 'percentage'

// Default batas threshold persentase anggaran
export const DEFAULT_BUDGET_THRESHOLD: number = 98

// Default interval update otomatis (menit)
export const DEFAULT_UPDATE_INTERVAL: number = 10

// Default nilai budget tambahan
export const DEFAULT_DAILY_BUDGET: number = 5000

// Default nilai budget tambahan
export const DEFAULT_EFFECTIVENESS_THRESHOLD: number = 20

// Default cooldown notifikasi (menit)
export const DEFAULT_NOTIFICATION_COOLDOWN: number = 5
